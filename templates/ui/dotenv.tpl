# __NAME__ UI — dev port and the two services it talks to.
#
# Read by vite.config.js, so no port is written in code. Change a number here
# and the dev server and every proxy target follow.

UI_PORT=__UI_PORT__
AUTH_URL=http://localhost:__AUTH_PORT__
API_URL=http://localhost:__API_PORT__
