-- The tables the picker lists and adds to: __MT_LEVEL_NAMES__.
--
-- Deliberately plain — an id and a name. Add what your app needs (an address,
-- a plan) with a later migration. Who may use one is NOT here: that is a
-- membership row in the sign-in database (userTenantsMapping), made for
-- whoever adds it.

__MT_TABLES_SQL__
